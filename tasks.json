{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Run iOS (Build+Launch)",
      "type": "shell",
      "command": "./run_sim.sh",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": []
    }
  ]
}
